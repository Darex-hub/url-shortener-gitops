apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kutt-ingress
  namespace: url-shortener
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - host: "54.174.176.188.nip.io"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kutt
                port:
                  number: 3000